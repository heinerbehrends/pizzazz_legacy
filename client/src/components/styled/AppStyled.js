import styled from 'styled-components';
import { spacer } from './styleConstants';

const AppWrapper = styled.div`
  text-align: center;
  padding: 6rem 0 ${spacer} 0;
  box-sizing: border-box;
  font-family: Roboto, sans-serif;
`;

export default AppWrapper;
